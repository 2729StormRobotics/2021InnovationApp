<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.stormroboticsnj">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Otium">
        <activity android:name=".shop2"></activity>
        <activity android:name=".customize2" />
        <activity
            android:name=".Shop"
            android:label="@string/title_activity_shop"
            android:theme="@style/Theme.Otium.NoActionBar" />
        <activity
            android:name=".Customize"
            android:label="@string/title_activity_customize"
            android:theme="@style/Theme.Otium.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>